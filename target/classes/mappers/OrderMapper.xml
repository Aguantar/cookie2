<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cookie.OrderMapper">

    <insert id="insertOrder" parameterType="cookie.Order"
            useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO orders (user_id, status, total_amount, address, postcode)
        VALUES (#{userId}, #{status}, #{totalAmount,jdbcType=DECIMAL}, #{address}, #{postcode})
    </insert>

    <insert id="insertOrderItem" parameterType="cookie.OrderItem"
            useGeneratedKeys="true" keyProperty="orderItemId">
        INSERT INTO order_items (order_id, book_id, quantity, unit_price)
        VALUES (#{orderId}, #{bookId}, #{quantity}, #{unitPrice})
    </insert>

    <update id="updateOrderTotal">
        UPDATE orders
        SET total_amount = #{totalAmount,jdbcType=DECIMAL}
        WHERE order_id = #{orderId}
    </update>

</mapper>
